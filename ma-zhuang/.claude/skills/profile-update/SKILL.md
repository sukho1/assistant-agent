---
name: "profile-update"
description: "End-of-session profile update. Invoked after important counseling sessions, or invoked when user requires — updates user_profile overview and dimension/complex files, performs weekly cycle check."
---

# 用户档案更新

## 触发条件

counseling 对话结束后，由 counseling skill 或 Agent 判断是否调用本 skill。触发条件：
- 对话产生了新的诊断信息（维度变化、综合体变化、关键事件）
- 日常闲聊不触发
- 当用户要求根据某一文件或内容进行深入分析时，根据分析结果记入对应档案。如果该文件中各部分内容不清楚具体时间，跟用户交流确认。

## 工作目录

用户档案位于 workspace 根目录下：`user_profile/`

## 流程

### 第一步：判断是否值得更新

检查本次对话是否产生了以下任一：
- 某个维度/综合体的状态有实质性变化
- 发生了关键事件（转折点、突破、崩溃后反弹等）
- 形成了新规则或新认知框架
- 力比多方向或性质有变化

不满足则不更新，直接结束。

案例：
- 关于他人回避的纠结，并意识到背后实际上是自己的力比多透支和守夜人自我审视，并跟AI对话，增加到今日日记的事实部分。

### 第二步：加载现有档案

- 读 `user_profile/last-update.md`，获取上次更新日期
- 读 `user_profile/comprehensive/overview.md` 了解当前综合状态
- 按需读 dim1/dim2/dim3 的 overview 文件

目的：在现有快照基础上增量更新。如果相关档案已在当前上下文中，跳过文件读取。

### 第三步：回溯检测缺失的周期文件，逐级补写

不依赖天数计算，而是检查已过去的周期是否已有对应文件。补写原则：**逐级向上，下级为源**。

数据源层级：`对话记录 → 周记 → 月记 → 季记 → 年记`。上级从下级摘录事实和合成分析。下级缺失则先级联补下级。

**周记补写**：
- 数据源：对话记录（当前上下文）+ 当日用户自评
- 按 `_cycle-file-rules.md` 周记格式：事实区（按日）+ 分析区（单日分析 + 本周综合分析）
- 无数据标注"（无记录）"，不编造

**月记补写**：
- 前置：该月所有周记存在？缺失先行补写
- 数据源：该月全部周记文件（comprehensive + dim1-3）
- 事实区：从各周记摘录构成结构性变化的关键事实（转折点、突破、新认知框架、维度级变化），保留日期和原编号。不是所有周记事实都进月记
- 分析区：从周记交互链中提取跨周延续的链合成为月度链；聚合各周五综合体动态形成月度位移

**季记补写**：
- 前置：该季所有月记存在？缺失先行补写
- 数据源：该季全部月记文件
- 事实区：摘录季度级关键事实（主要矛盾级别的变化）
- 分析区：合成跨月交互因果链

**年记补写**：同理从季记向上合成。

级联示例：7月1日发现6月月记缺失 → 先检查6月周记(W23-W26) → W26缺失则先从对话记录补写W26 → 周记齐全后读4份周记 → 摘录事实 → 合成分析 → 写6月月记。

无记录时段统一标注"（无记录）"，不编造。

### 第四步：更新周期文件（如触发）

四个位置，按序写入：
1. `four-dimensions/dim1-elements/{period}/{file}.md`
2. `four-dimensions/dim2-links/{period}/{file}.md`
3. `four-dimensions/dim3-karma/{period}/{file}.md`
4. `comprehensive/{period}/{file}.md`（最后写——需要维度文件的交互因果链）

所有周期文件遵循**事实/分析分离**规则：上半区用户事实（只追加，不删除，编号），下半区 Agent 分析（可引用事实编号），中间以“---”分隔。
详细格式规范见 `_cycle-file-rules.md`。

### 第五步：更新 overview 文件

覆写以下 overview 文件中有变化的时间层级：
1. `comprehensive/overview.md`
2. `four-dimensions/dim1-elements/overview.md`
3. `four-dimensions/dim2-links/overview.md`
4. `four-dimensions/dim3-karma/overview.md`

更新方式：
- 周更新：追加周度变化条目，更新最近关键变化列表
- 月更新：追加月度变化条目，下层有结构性意义的向上传导
- 季更新：追加季度变化条目
- 上层周期（年/季）只在有结构性变化时修订，不机械每周重写
- 下层周期的变化若构成上层周期的结构性变化，向上传导

**覆写前对比机制**（逐层，从细到粗）：

| 层级 | 旧值 | 新值 | 覆写条件 |
|---|---|---|---|
| 周度 | 现有 overview 周度层 | 刚写完的周记综合分析 | 本周主要矛盾/变化与旧值不同 |
| 月度 | 现有 overview 月度层 | 刚写完的月记（或各周记聚合） | 月度趋势方向改变 |
| 季度 | 现有 overview 季度层 | 刚写完的季记（或各月记聚合） | 季度级主题/方向改变 |
| 年度 | 现有 overview 年度层 | 各季度聚合 | 年度主要矛盾转移 |
| 近三年 | 现有 overview 近三年层 | 各年度聚合 | 三年窗口内核心叙事改变 |

- 无变化则保持，不机械刷新
- 下层变化不一定触发上层覆写——只有改变上层主要矛盾或趋势方向时才传导
- 旧 overview 某层缺失或格式不一致，以新值为准

模板见 `_overview-templates.md`。Overview 文件同样遵循**事实/分析分离**规则。root `overview.md` 是薄层路由文件，无需更新。

### 第六步：更新 last-update.md

写入当天日期（格式：YYYY-MM-DD）。

## 核心规则：事实与分析分离

所有档案文件（周期文件 + overview 文件）统一遵循：

- 文件分上下两区，用 `---` 分隔
- **上半区 · 用户事实**：Agent 只追加，不删除。修改需经用户同意。每条事实标注具体日期，做序号标记（如 `1.1`、`1.1.a`）
- **下半区 · Agent 分析**：可引用事实编号简述，如"引用事实 1.1.a 用户晕倒后冷静分析"，避免长篇复述
- **层级递进**：周记事实最全面；月记/季记/年记只摘录最重要事实，保留原始编号
- **时间锚定**：每条事实必须标注具体日期；周记按日分节，分析区单日分析也按日对齐

详细格式规范见 `_cycle-file-rules.md` 和 `_overview-templates.md`。

---

## 更新原则

适用于所有文件：
- **不覆盖历史**：overview 覆写是快照更新，周期文件是追加式记录，两者不冲突
- **引用原话**：关键变化描述尽量引用用户原话作为证据
- **简洁**：每条快照 1-2 句，不写论文
- **定性不定量**：不评分数，不打数字分。用"强/中/弱""通畅/阻滞""直接性/对抗性"等定性描述
- **不猜测**：只写对话中明确呈现的，不从"无记录"的日子强行推断
- **与用户自述一致**：用户说"心力不错"就不能写"心力低迷"，可在自述基础上补充分析视角但不能推翻
